#include <stdio.h>
int average (int x, int y, int z) {
printf("The three numbers are ") ;
scanf("%d%d%d", &x, &y, &z) ;
printf(" The average of given numbers is %d",((x+y+z) /3));
}
int main () {
int a, b, c;
average(a, b, c) ;

return 0;
}


    